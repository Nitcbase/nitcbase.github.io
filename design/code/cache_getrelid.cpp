relId OpenRelTable::getRelId(char relName[ATTR_SIZE]){
	//Iterate over the relation cache{
		//if relation with relation name relName is opened in the relation cache
			return relid //return corresponding relid
	//}
	return E_NOTOPEN;
} 
